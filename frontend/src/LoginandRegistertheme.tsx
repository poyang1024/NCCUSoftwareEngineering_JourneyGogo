import { createTheme } from '@mui/material/styles'

const LoginandRegistertheme = createTheme({
    typography: {
      h5: {
        fontSize: 20,
        fontWeight: 500,
        fontFamily: "Noto Sans TC",
      },
    },
  });

export default LoginandRegistertheme